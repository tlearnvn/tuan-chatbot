# Cấu hình thật (chứa mật khẩu CSDL và khoá mã hoá)
/config/config.php

# Thư mục tệp cũ (nếu nâng cấp từ bản trước 1.1.0).
# Bản mới lưu tệp trong cơ sở dữ liệu nên không sinh thư mục này.
/uploads/

# Bản đóng gói phát hành
/dist/
*.zip

# Rác của hệ điều hành / IDE
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp
